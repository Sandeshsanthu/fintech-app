# Code Generated by Sidekick is for learning and experimentation purposes only.
server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://mysql-user-db:3306/user_service_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: ${MYSQL_ROOT_PASSWORD:shantanu@123}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret: ${JWT_SECRET:my-super-secret-key-that-you-never-share}

# Wallet service URL (using Docker service name)
wallet:
  service:
    url: http://wallet-service:8088
